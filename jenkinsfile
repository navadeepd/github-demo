pipeline {
    agent any
    stages {
        stage('build') {
            steps {
                sh 'git --version'
            }
			}
			
			stage('deploy') {
            steps {
                sh 'java --version'
            }
        }
    }
}
